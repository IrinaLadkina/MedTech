package com.example.MedTech.repo;

import com.example.MedTech.domain.ActiveRequestCheckpoint;
import org.springframework.data.repository.CrudRepository;

public interface ActiveRequestCheckpointRepo extends CrudRepository<ActiveRequestCheckpoint, Long> {
}
